"use strict";
/* Intentional Error: If you haven’t received an array index error in one of your programs yet, try to
make one happen. Change an index in one of your programs
to produce an index error. Make sure you correct the error before closing the program. */
// Array only has 5 elements, so this will produce an index error. 
const numbers = [1, 2, 3, 4, 5];
console.log(numbers[10]);
// No Index erroe because index 2 is present
const number = [1, 2, 3, 4, 5];
console.log(number[2]);
